<Page x:Class="QuanLyCaPheApp.Views.BaoCaoView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:QuanLyCaPheApp.ViewModels"
      xmlns:conv="clr-namespace:QuanLyCaPheApp.Converters"
      Title="Xuat Bao Cao" Background="{StaticResource BackgroundBrush}">
    <Page.DataContext><vm:XuatBaoCaoViewModel/></Page.DataContext>
    <Page.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <conv:NumberFormatConverter x:Key="NumFmt"/>
    </Page.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Xuat Bao Cao" Style="{StaticResource SectionHeader}"/>

        <!-- Filter + Actions -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Tu ngay:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <DatePicker Grid.Column="1" SelectedDate="{Binding TuNgay}" Height="34" Margin="0,0,12,0"/>
                <TextBlock Grid.Column="2" Text="Den ngay:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <DatePicker Grid.Column="3" SelectedDate="{Binding DenNgay}" Height="34" Margin="0,0,12,0"/>
                <Button Grid.Column="4" Content="Xem Bao Cao" Style="{StaticResource PrimaryButton}"
                        Height="34" Command="{Binding XemBaoCaoCommand}" Margin="0,0,8,0"/>
                <Button Grid.Column="5" Content="Xuat Excel (.xlsx)" Style="{StaticResource SuccessButton}"
                        Height="34" Command="{Binding XuatExcelCommand}" Margin="0,0,8,0"
                        IsEnabled="{Binding DaDangTai, Converter={StaticResource BoolToVis}, ConverterParameter=invert}"/>
                <TextBlock Grid.Column="7" Text="{Binding ThongBao}" VerticalAlignment="Center"
                           Foreground="{StaticResource SuccessBrush}" TextWrapping="Wrap" MaxWidth="200"/>
            </Grid>
        </Border>

        <!-- Report preview header -->
        <Border Grid.Row="2" Background="{StaticResource PrimaryBrush}" CornerRadius="6,6,0,0"
                Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="BAO CAO DOANH THU QUAN CA PHE" FontSize="15" FontWeight="Bold"
                               Foreground="White"/>
                    <TextBlock Text="{Binding TuNgay, StringFormat='Tu: dd/MM/yyyy'}"
                               Foreground="#BCAAA4" FontSize="12"/>
                    <TextBlock Text="{Binding DenNgay, StringFormat='Den: dd/MM/yyyy'}"
                               Foreground="#BCAAA4" FontSize="12"/>
                </StackPanel>
                <TextBlock Grid.Column="1" Text="Phien ban 1.0" Foreground="#BCAAA4"
                           FontSize="11" VerticalAlignment="Bottom"/>
            </Grid>
        </Border>

        <!-- Data table -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding DanhSach}"
                  Style="{StaticResource ModernDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ngay" Binding="{Binding NgayText}" Width="110"/>
                <DataGridTextColumn Header="So Hoa Don" Binding="{Binding SoHoaDon}" Width="100"/>
                <DataGridTextColumn Header="Doanh Thu Goc" Binding="{Binding DoanhThuGoc, Converter={StaticResource NumFmt}}" Width="*"/>
                <DataGridTextColumn Header="Tong Giam Gia" Binding="{Binding TongGiamGia, Converter={StaticResource NumFmt}}" Width="*"/>
                <DataGridTextColumn Header="Thuc Thu" Binding="{Binding ThucThuText}" Width="*"/>
                <DataGridTextColumn Header="So Khach Hang" Binding="{Binding SoKhachHang}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>

<Page x:Class="QuanLyCaPheApp.Views.TrangChuView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:conv="clr-namespace:QuanLyCaPheApp.Converters"
      Title="Trang Chủ - Sơ Đồ Bàn"
      Background="{StaticResource BackgroundBrush}">
    <Page.Resources>
        <conv:StatusToColorConverter x:Key="StatusColor"/>
    </Page.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Trang Chủ - Sơ Đồ Bàn" Style="{StaticResource SectionHeader}" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="🔄 Làm Mới" Style="{StaticResource SecondaryButton}"
                    Command="{Binding RefreshCommand}"/>
        </Grid>

        <!-- Thống kê nhanh -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource StatCard}">
                <StackPanel>
                    <TextBlock Text="Doanh Thu Hôm Nay" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Text="{Binding DoanhThuText}" FontSize="22" FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrush}" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource StatCard}">
                <StackPanel>
                    <TextBlock Text="🧾 Số Hóa Đơn" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Text="{Binding SoHoaDonHomNay}" FontSize="22" FontWeight="Bold"
                               Foreground="{StaticResource InfoBrush}" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource StatCard}">
                <StackPanel>
                    <TextBlock Text="🪑 Bàn Đang Dùng" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Text="{Binding SoBanDangDung}" FontSize="22" FontWeight="Bold"
                               Foreground="{StaticResource DangerBrush}" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Style="{StaticResource StatCard}" Margin="0">
                <StackPanel>
                    <TextBlock Text="Khách Mới Hôm Nay" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Text="{Binding SoKhachHangMoi}" FontSize="22" FontWeight="Bold"
                               Foreground="{StaticResource SuccessBrush}" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Chú thích + Lọc khu vực -->
        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Chú thích: " FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                <Border Background="#4CAF50" Width="18" Height="18" CornerRadius="3" Margin="4,0"/>
                <TextBlock Text="Trống " FontSize="12" VerticalAlignment="Center" Margin="3,0,10,0"/>
                <Border Background="#F44336" Width="18" Height="18" CornerRadius="3" Margin="4,0"/>
                <TextBlock Text="Đang dùng " FontSize="12" VerticalAlignment="Center" Margin="3,0,10,0"/>
                <Border Background="#FFC107" Width="18" Height="18" CornerRadius="3" Margin="4,0"/>
                <TextBlock Text="Đã đặt " FontSize="12" VerticalAlignment="Center" Margin="3,0,10,0"/>
                <Border Background="#9E9E9E" Width="18" Height="18" CornerRadius="3" Margin="4,0"/>
                <TextBlock Text="Bảo trì" FontSize="12" VerticalAlignment="Center" Margin="3,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <TextBlock Text="Khu vực: " VerticalAlignment="Center" FontSize="12" Margin="0,0,8,0"/>
                <ComboBox ItemsSource="{Binding DanhSachKhuVuc}"
                          SelectedItem="{Binding FilterKhuVuc}"
                          Width="140" Style="{StaticResource ModernComboBox}"/>
            </StackPanel>
        </Grid>

        <!-- Sơ đồ bàn dạng lưới -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding DanhSachBan}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource TableButton}"
                                Background="{Binding MauTrangThai}"
                                Command="{Binding DataContext.ChonBanCommand,
                                          RelativeSource={RelativeSource AncestorType=Page}}"
                                CommandParameter="{Binding}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
